# Core runtime (the symbolic baseline runs on CPU without torch)
numpy>=1.21.0
gymnasium>=0.29.0
opencv-python>=4.5.0
omegaconf>=2.2.0
hydra-core>=1.2.0
pyyaml>=6.0
requests>=2.28.0
wandb>=0.15.0

# Optional: neural models, FP8 inference (pip install -e ".[ml]")
# torch>=2.0.0
# transformers>=4.20.0
# pillow>=9.0.0

# Development
pytest>=7.0.0
