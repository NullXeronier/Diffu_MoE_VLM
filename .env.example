# WandB Environment Configuration
# Copy this file to .env and configure your WandB settings

# WandB API Key (get from https://wandb.ai/settings)
WANDB_API_KEY=your_api_key_here

# WandB project and entity settings
WANDB_PROJECT=diffu-moe-vlm-minecraft
WANDB_ENTITY=your_username_or_team

# WandB mode: online, offline, or disabled
WANDB_MODE=online

# Optional: Set to "allow" for anonymous logging
# WANDB_ANONYMOUS=allow

# Optional: Disable WandB completely
# WANDB_DISABLED=true

# Optional: Set custom run name
# WANDB_RUN_NAME=my_experiment_run

# Optional: Set custom notes
# WANDB_NOTES="Experiment with new hyperparameters"

# Optional: Set tags (comma-separated)
# WANDB_TAGS=experiment,minecraft,vlm

# Local LLM (OpenAI-compatible endpoint; base URL includes /v1)
# These override the `llm:` section in configs/defaults.yaml
LLM_API_BASE=http://localhost:8000/v1
LLM_MODEL=local-llama3
LLM_API_KEY=DUMMY
